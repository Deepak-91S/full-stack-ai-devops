name: Demo CI Env Secrets

on: 
    # push
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Say Hello
              run: echo "Hello from the second workflow!"

            - name: Testing Env values
              env:
                APP_ENV: demo_env
              run: echo "Hi How is your project $APP_ENV"
        