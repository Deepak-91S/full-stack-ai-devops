apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: java-app-pod
  template:
    metadata:
      labels:
        app: java-app-pod
    spec:
      containers:
        - name: myjava-app-con
          image: deepak3006/hello-spring:latest
          env:
            - name: LOG_LEVEL
              value: "Development"
            - name: APP_COLOR
              value: "green"
          # env:
          #   - name: PORT
          #     valueFrom:
          #       configMapKeyRef:
          #         name: appl-config
          #         key: PORT
          #   - name: ENV_VALUE
          #     valueFrom:
          #       configMapKeyRef:
          #         name: appl-config
          #         key: ENV_VALUE
          ports:
            - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: java-app-serv
spec:
  selector:
    app: java-app-pod
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
